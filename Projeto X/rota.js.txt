import express from ' express'
import path from 'path'
import Crud from 'crus.js'



const express = require('express')
const path = require('path')
const app = espress()
const port = 3000

app.get('/selecionar', (req,res) => {

	//Instância
	const cd = Crud()

	//chamar o método
	cd.inserir()
})



app.listen(port,() => {
	console.log(`http://localhost:${port}`)
})